# 更新记录

本项目使用语义版本；alpha 版本表示仍在实机验收阶段，不能按正式生产版本对待。每次更新保留变更记录、测试证据和未验证范围。

## 未发布修复 — 2026-09-25

- 宿主 Git 与配置检查统一使用仓库外空 hook 目录；异常时失败关闭，工作树中的 hooks 不再由宿主触发。
- 补充要求在异步整合、提交和审查期间不会被旧任务对象覆盖；过期版本的结果不会参与新版本整合。
- 审查连续两轮拒绝后，继续会唤醒发起方进入整合修复，并从新修复周期计数。
- 提交与审查请求绑定产生它们的 Codex 轮次；失败轮次不执行请求，重启时先核对持久事件及本地/远端 SHA。
- 只有中转确认接纳后才能启动 Agent；同时发起由中转选出唯一共享任务，被拒绝候选不会启动。
- 修正 Git 引用故障交接说明，移除手动写 `.git/refs` 的危险步骤。

## 2.0.0-alpha.2 — 2026-09-25

### 产品改造

- 以黑白聊天界面取代 v1.1 工作台，保留浅/深/系统主题、会话、连接向导与内联权限请求。
- 改用本机 Codex App Server、持久线程及显式协作工具，新增两人房间中转、收发去重和取消记录。
- 双方独立工作树、专用 cm 分支同步、发起方集成和精确 SHA 同伴审查。
- 同伴进展增量同步与有预算的上下文注入，退出旧 Issue/Approval/PR 主流程。

### 本轮修复

- Electron ESM 启动卡死；建线程和审查期间停止后任务可能恢复；停止后的权限请求失效。
- 手动上下文收发格式、长历史分页、积压上下文总预算、导入提示词回传放大。
- 用户补充导致旧审查过期；脏工作树不能认证原 SHA；审查方暂停后的恢复。
- 超大 WebSocket 帧异常、旧连接重连与过期回调；构建清理旧版编译残留。
- macOS 按宿主架构打包；清理锁文件中已删除旧版依赖的残留声明。
- 补齐产品、技术、部署、进度、验收和交接文档，纠正中转可见性说明。

### 验证与限制

23 项自动化测试、2 项浏览器测试、构建通过；真实 Windows Electron 窗口与登录态、真实 Codex 独立线程上下文召回通过。Windows 安装包供测试使用，未配置发行签名。两账号跨设备全流程、macOS 真机和异地 TLS 部署仍未验收。

## 2.0.0-alpha.1 — 开发检查点

本地极简聊天与上下文同步初始实现，未宣称完整跨机验收。

## 1.1.0-rc.1 — 历史工作台

旧产品在 `pre-chat-redesign-20260924` 检查点保留。当前新聊天数据库不覆盖旧数据库。
