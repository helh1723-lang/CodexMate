# 验收记录

更新：2026-09-25。版本 2.0.0-alpha.2 不代表正式交付验收完成。

## 本轮实际证据

| 层次 | 结果 | 证据/命令 |
| --- | --- | --- |
| 类型检查 | 通过 | `npm run typecheck` |
| 单元与集成 | 23/23 通过 | `npm test`；含 10 项本轮新增回归 |
| 编译与前端构建 | 通过 | `npm run build` |
| 双客户端 + 真 Git | 通过，可控 Agent | `tests/chat.test.ts`：双方写实际文件、消息影响后续执行、commit/push、整合、同伴检查、相同成果 SHA |
| 浏览器 | 2/2 通过，Windows Edge | `npm run test:ui`：浅/深主题、持久化、向导、键盘和窄屏 |
| 真实 Electron | 源码及打包程序窗口通过 | 本机 `artifacts/desktop-smoke.json`：窗口、ChatGPT 登录态、contextIsolation=true、nodeIntegration=false、sandbox=true |
| 真实 Codex 上下文 | 通过，同机同账号两个线程 | `artifacts/context-verify.json`：源线程实际写文件，导出 4 条；接收线程使用独立空目录、0 条命令，准确召回标记 |
| 真实 Codex 基础执行 | 上轮通过 | `artifacts/codex-smoke.json`：写文件、回读、动态工具、完成；该文件是历史 smoke 证据 |
| Windows 安装包 | NSIS 已构建，打包程序启动通过 | `npm run dist:win`；输出 `artifacts/desktop/`，未测试系统安装/卸载，未配置签名 |

## 新增回归覆盖

公开脱敏摘要见 [verification-summary.json](../artifacts/verification-summary.json)。原始报告可能包含本机路径，保留在本机且不上传 GitHub。

手动同步收发契约且不唤醒；审查中停止会中断检查且不回写完成；停止使权限失效；建线程中停止不启动 turn；脏工作树不能通过原 SHA 审查；多 bundle 合计 UTF-8 预算与不回传导入提示词；长会话读取最新页；补充要求使旧审查失效；超大 WebSocket 帧只关闭客户端。

## 尚未验收

| 验收项 | 状态与需要的环境 |
| --- | --- |
| 两账号真实模型完整任务 | 未做；需要两个独立账号参与，不可用模拟 Agent 替代 |
| Windows↔Windows 异地 | 未做；第二台机器及 HTTPS 中转 |
| macOS↔macOS 异地 | 未做；两台 Mac 与独立账号 |
| Windows↔macOS 异地 | 未做；跨平台设备和实际 Git 远端 |
| macOS DMG 与架构依赖 | 只有配置，未实际构建/安装，需在 macOS CI 或真机验证 |
| Docker/Caddy TLS | 模板和操作文档已提供，本轮没有部署服务器 |
| 真实网络故障矩阵 | 重复投递/离线取消有本机自动化；仍需在真机验证断网、重启、权限拒绝、冲突、检查失败和预算恢复 |
| 大规模长对话 UI | 有结构化预算和折叠展示，尚未做大型历史性能验收 |
| 签名、公证、安装升级/卸载 | 未配置发行签名、公证；没有宣称正式分发就绪 |

## 两机验收步骤

1. 两侧克隆同一实际远端，配置可信检查命令，各自登录各自账号。
2. 连接共同 HTTPS 中转并双方一次授权，记录平台、版本、网络、基准 SHA。
3. A 只输入一次目标；保存双方实际修改、至少一次影响代码的消息、检查输出、最终成果 SHA 及同伴审查。
4. 再测试运行中补充、权限拒绝、暂停/继续、任一方停止、离线端恢复优先取消。
5. 每种平台组合独立填写结果与日志，不能把本机多进程结论复制为跨机通过。

不再沿用历史交接中的固定额度重置时间和代理端口：它们不是当前事实。发生连接错误需检查当前环境，不能一律断言是代理或额度。
